<?xml version="1.0" encoding="utf-8"?>
<!--
🔹 activity_home.xml

This layout file represents the main UI for the Home screen.
Each UI element is fully connected with its functionality:
- Toolbar → displays the title and logout button
- FragmentContainerView → holds the fragments (NavHost)
- BottomNavigationView → bottom navigation menu
-->
<androidx.appcompat.widget.LinearLayoutCompat
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">

    <!-- 🔹 FragmentContainerView: used to host child fragments -->
    <androidx.fragment.app.FragmentContainerView
        android:id="@+id/home_nav_host_fragment"
        android:name="androidx.navigation.fragment.NavHostFragment"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        app:navGraph="@navigation/home_nav_graph"
        app:defaultNavHost="false"/>

</androidx.appcompat.widget.LinearLayoutCompat>